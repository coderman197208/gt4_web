## 1. Shared Navigation Metadata

- [x] 1.1 提取 `AppSidebar` 当前菜单的共享导航配置，统一维护文案、路由路径、admin 权限、页脚迁移标记和显示顺序。
- [x] 1.2 重构 `AppSidebar.vue` 以复用共享导航配置，并保持现有侧边栏的关闭行为、激活态和 admin 可见性不变。

## 2. HomePage Footer Shell

- [x] 2.1 在 `HomePage.vue` 中加入固定页脚层，调整壳层纵向布局，确保新增页脚后仍保持全屏、无浏览器滚动。
- [x] 2.2 在页脚左侧实现基于 10 等宽槽位的连续导航带，按顺序接入主监控、合同数据编辑、参数设定、格式设定、管捆编辑、报警管理 6 个迁移入口，并为剩余槽位提供非交互占位。

## 3. Styling And Access Behavior

- [x] 3.1 让页脚按钮带、激活态、悬停态和占位单元对齐 `MainMonitorView` 的浅灰工业 HMI 风格，形成连续无缝的底部导航观感。
- [x] 3.2 在页脚右侧实现“设备部设控中心”品牌文案区，并处理非 admin 场景下报警管理槽位的不可交互表现，保证布局顺序稳定且不绕开现有权限语义。

## 4. Validation

- [x] 4.1 运行受影响前端范围的类型检查或等效静态校验，确认共享导航配置与页脚改动未引入模板、类型或导入错误。
- [x] 4.2 在 `main-monitor`、`contract-editing`、`parameter-setting` 等页面下手动验证页脚显示、按钮跳转、激活态、无滚动布局，以及 admin / 非 admin 两类用户的报警管理槽位表现。
